
---

# 🎯 **三大核心模块（三个人都写“硬核逻辑”）**

```
A：任务管理核心（链表 + 增删改查）
B：数据持久化（文件格式 + 加载 + 保存）
C：业务逻辑（排序算法 + 分类 + 状态更新）
```

三者相互调用，但工作内容**完全并发**，互相不等待。

---

# 🧩 **A 组：任务核心模块（数据结构 & 操作）**【最重要】

负责文件：

```
task.h
task.cpp
```

负责所有 Task 结构相关的“底层”功能：

### 🟦 Task 数据结构

* struct Task 定义
* next 指针、链表结构

### 🟦 链表操作（真正的核心）

* createTask()
* insertTask()
* deleteTask()
* findTask()
* updateTask()

### 🟦 单链表维护

* 获取链表长度
* 遍历
* 合并链表（可能用于排序）

### 🟦 必须提供给其他组的接口（API）

A 组负责写这些函数，其他组只调用，不关心内部实现：

```cpp
Task* createTask(...);
void insertTask(Task*& head, Task* node);
bool deleteTask(Task*& head, int id);
Task* findTask(Task* head, int id);
void updateTask(Task* task, ...);
void clearList(Task*& head);
```

**A 组产出的是“操作链表的基础工具”，三人中最算法向的人来做。**

---

# 🧩 **B 组：IO & 数据持久化（文件序列化/反序列化）**【难度不低】

负责文件：

```
io.h
io.cpp
```

主要逻辑：

### 🟦 文件格式设计

文本格式建议：

```
id|title|priority|deadline|state|description
```

### 🟦 数据加载 loadData()

* 打开文件
* 一行一行解析
* 调用 A 组的 createTask → insertTask
* 建立 allList 链表

### 🟦 数据保存 saveData()

* 遍历 allList
* 输出到文件

### 🟦 字符串解析

* split()
* 去空格
* 处理换行符

### 🟦 字段校验

* deadline 是否有效
* id 冲突如何处理

此模块工作量也非常大，而且很“工程感”。

---

# 🧩 **C 组：业务规则（分类 + 状态管理 + 排序）**【算法最多的一组】

负责文件：

```
biz.h
biz.cpp
（原本 classify + sort 合并）
```

业务逻辑本质是一套算法，因此 C 组工作量非常可观，绝对不比 A、B 少。

负责内容：

---

## ① 日期处理（非常核心）

* 日期格式检查
* 日期比较 compareDate()
* 判断是否 overdue / today / this week

需要写：

```cpp
int compareDate(string d1, string d2);
int daysBetween(string d1, string d2);
bool isSameDay(string d1, string d2);
bool isSameWeek(string d1, string d2);
```

---

## ② 分类（业务逻辑核心）

输入：allList
输出：todayList, weekList, overdueList

流程逻辑：

```
for each task in allList:
    if unfinished and deadline < today → overdueList
    else if deadline == today → todayList
    else if deadline 在本周 → weekList
```

---

## ③ 排序（算法模块）

链表排序，可选择：

* 冒泡排序（简单）
* 插入排序（链表友好）
* 归并排序（你想高分的话用这个）

实现：

* sortByDeadline()
* sortByPriority()
* sortOptimal()

---

# 🎯 **为什么这样拆刚刚好？**

### 1. 三人都写“核心逻辑”

不是某个人只写主菜单这种轻量模块。

### 2. 三个模块几乎互不依赖，可以同时写

* A 提供链表接口
* B 调用 A 生成任务节点
* C 调用 A 遍历、调用 B 加载完的数据

互相不会卡住。

### 3. 三个模块都能写很多代码（量均衡）

A 有链表算法
B 有文件解析 + 字符串处理
C 有分类算法 + 排序算法 + 日期处理

没有一个模块是轻量的。

### 4. 难度由浅到深

你们可以按照能力分配：

| 角色  | 难度         | 推荐给谁     |
| --- | ---------- | -------- |
| A 组 | 中等难度（链表）   | 算法基础不错的人 |
| B 组 | 中等偏上（解析）   | 工程感强的人   |
| C 组 | 最复杂（排序、算法） | 班里最强的人   |

---

# 🎯 C 组最后写 main（整合者）

当 A/B/C 完成各自模块后：

✔ C 组再写 main.cpp
✔ 调用三人的接口
✔ 写菜单
✔ 写显示逻辑

这样 C 组工作量会很大，但不是简单，而是 **最终总线控制模块 + UI + 整合工程的核心**。

因此 C 组并不简单，只是“最后进行”。

---
